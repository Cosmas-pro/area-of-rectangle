/*calculate the area of a rectangle*/
#include<stdio.h>
int main()
{
int length, width, Area;
printf("\n Enter value to length:");
scanf("%d",&length);
printf("\n Enter value to width:");
scanf("%d",&width);
Area =length*width;
printf("\n Area =&",Area);
return 0;
}
